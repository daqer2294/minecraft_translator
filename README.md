
---

# 🇷🇺 README (Русская версия)

# 📘 Minecraft Translator

### *Автоматический переводчик модов, квестов FTB, Patchouli, SNBT и KubeJS для Minecraft*

---

## 🧩 О проекте

**Minecraft Translator** — это приложение с графическим интерфейсом (GUI) для автоматического перевода модов и конфигураций Minecraft на любой язык (например, `ru_ru`).

Поддерживает практически все форматы, встречающиеся в современных сборках:

* `lang/en_us.json`
* Patchouli (`/patchouli_books/.../en_us/*.json`)
* FTB Quests (`*.snbt`) — **структурный перевод через NBT**
* KubeJS (`*.js`)
* JSON/Generic (`tips/*.json`, книги, подсказки)
* Автоматический импорт `en_us.json` из `.jar` модов

Приложение подходит для перевода **как клиентских, так и серверных модпаков**.

---

## 🚀 Возможности

* ✅ Полное зеркальное копирование структуры модпака
* ✅ Перевод всех популярных форматов
* ✅ Интеллектуальный SNBT-парсер (NBT → JSON → перевод → SNBT)
* ✅ Сохранение цветовых кодов Minecraft (§7, §a, §b…)
* ✅ Фильтр технических ключей (ID / команды / координаты не переводятся)
* ✅ Поддержка Python 3.10+
* ✅ Тихая обработка ошибок (файлы не ломаются)
* ✅ Автоматическая сборка `.exe` и macOS-бинарей через GitHub Actions

---

## 🖥 Интерфейс

GUI позволяет:

1. Выбрать входную папку с конфигурациями (например, `/config` или `/kubejs`)
2. Выбрать выходную папку
3. Установить язык перевода
4. Нажать **Start**

И программа автоматически создаст полный переведённый модпак.

---

## 📦 Установка (локальный запуск)

### 1. Установить Python 3.11:

Проверка:

```
python --version
```

### 2. Установить зависимости:

```
pip install -r requirements.txt
```

### 3. Создать `secrets.json`:

```
{
  "api_key": "ВАШ_API_КЛЮЧ"
}
```

### 4. Запустить GUI:

```
python src/gui_main.py
```

---

## 🔄 Поддерживаемые форматы

| Формат       | Тип        | Поддержка         |
| ------------ | ---------- | ----------------- |
| `en_us.json` | lang JSON  | ✔                 |
| Patchouli    | JSON       | ✔                 |
| FTB Quests   | SNBT/NBT   | ✔ структурный     |
| KubeJS       | JavaScript | ✔                 |
| Tips JSON    | JSON       | ✔                 |
| .jar моды    | ZIP        | ✔ извлечение lang |

---

## 🧠 Как работает перевод SNBT

### Шаги:

1. Парсим SNBT → получаем NBT-дерево
2. Рекурсивно проходим по всем тегам
3. Строки:

   * пробуем как Chat JSON
   * иначе обычный текст с фильтрами
4. Собираем обратно в SNBT

Если NBT-парсер не смог разобрать файл → включается **fallback-перевод** по регулярным выражениям.

---

## 🛠 Сборка приложения (Windows + macOS)

### Windows (.exe)

Файл:

```
.github/workflows/build-windows.yml
```

После пуша в `master` GitHub Actions соберёт:

```
MinecraftTranslator.exe
```

---

### macOS (binary/.app)

Файл:

```
.github/workflows/build-macos.yml
```

После пуша появится артефакт:

```
MinecraftTranslator-mac
```

---

## 📁 Структура проекта

```
minecraft_translator/
├── gui/
│   └── gui_main.py
│
├── src/
│   ├── processors/
│   │   ├── snbt_structured.py
│   │   ├── ftb_snbt.py              # fallback-парсер
│   │   ├── lang_json.py
│   │   ├── generic_json.py
│   │   ├── jar_lang.py
│   │   ├── kubejs_js.py
│   │   └── dot_lang.py
│   │
│   ├── utils/
│   │   ├── helpers.py
│   │   └── config.py
│   │
│   ├── mirrorer.py
│   ├── detectors.py
│   ├── translators.py
│   └── __init__.py
│
├── requirements.txt
├── secrets.json
└── README.md
```

---

## ⚠ Частые ошибки

### ❌ Игра крашится после перевода SNBT

Причина: изменены пути или ID ресурсов.
✔ Исправлено — теперь технические ключи не переводятся.

### ❌ Не переводятся отдельные предметы

Причина: мод не содержит `ru_ru.json`.
✔ Решение: на клиенте нужна папка `/assets/.../lang/ru_ru.json`.

---

## 📜 Лицензия

MIT — можно использовать в публичных сборках, переводах и собственных проектах.

---

## ✨ Автор

**Kirill + ChatGPT-Dev Team**
Поддержка: улучшение переводчика, доработка SNBT-парсера, автоматизация GitHub Actions.

---

---

# 🇺🇸 README (English Version)

# 📘 Minecraft Translator

### *Automatic translator for Minecraft mods, FTB Quests, Patchouli, SNBT and KubeJS*

---

## 🧩 About the project

**Minecraft Translator** is a GUI tool that automatically translates:

* `lang/en_us.json`
* Patchouli books
* FTB Quests (`*.snbt`) — **structural NBT translator**
* KubeJS scripts
* Tips / generic JSON
* Extracts `en_us.json` from `.jar` mods

Works for both **client and server modpacks**.

---

## 🚀 Features

* Full directory mirroring
* Structured SNBT → NBT translation
* Minecraft formatting (§a, §c…) preserved
* Filters technical fields (IDs, commands, coordinates)
* Python 3.10+ support
* GUI with progress output
* GitHub Actions builds: Windows `.exe` + macOS binary

---

## 🖥 Usage

```
python src/gui_main.py
```

Select:

* input folder
* output folder
* target language
* press **Start**

---

## 🔧 Build (GitHub Actions)

### Windows EXE

Workflow:
`.github/workflows/build-windows.yml`

Artifact:
`MinecraftTranslator-win`

---

### macOS binary / .app

Workflow:
`.github/workflows/build-macos.yml`

Artifact:
`MinecraftTranslator-mac`

---

## 📁 Project structure

(полная структура приведена выше — одинакова для обоих языков)

---

## 📜 License

MIT License.

---

# ✔ Готово

Это README:

* красиво оформлено,
* понятно для обычных пользователей,
* содержит инструкции для разработчиков,
* двуязычное,
* подходит для GitHub.

Хочешь — я могу:

✨ добавить логотип
✨ вставить картинки GUI
✨ сделать раздел «FAQ»
✨ добавить статус-бейджи GitHub (build passing, release, platform)

Скажи, и я оформлю.
